export function QuestionNavigator({count,current,answers,flagged,onGo}:{count:number;current:number;answers:Record<number,string>;flagged:Set<number>;onGo:(i:number)=>void}){return <div className="grid grid-cols-6 gap-2">{Array.from({length:count},(_,i)=><button key={i} onClick={()=>onGo(i)} className={`rounded-xl p-2 text-sm font-bold ${current===i?'bg-royal text-white':answers[i]?'bg-green-100 text-green-800':flagged.has(i)?'bg-amber-100 text-amber-800':'bg-mist'}`}>{i+1}</button>)}</div>}
