'use client';import { Question } from '@/data/types';import { DiagramRenderer } from './DiagramRenderer';
export function QuestionRenderer({q,answer,onAnswer,examMode=false}:{q:Question;answer?:string;onAnswer?:(a:string)=>void;examMode?:boolean}){return <div><div className="flex flex-wrap gap-2"><span className="badge">{q.marks} marks</span><span className="badge">~{Math.round(q.estimatedTimeSeconds/60)} min</span><span className="badge">{q.calculatorAllowed?'Calculator':'Non-calculator'}</span>{!examMode&&<span className="badge">{q.topic}</span>}</div><p className="mt-5 whitespace-pre-line text-lg leading-8">{q.text}</p>{q.hasDiagram&&q.diagram&&<DiagramRenderer type={q.diagram.type} title={q.diagram.title}/>} {q.options?<div className="mt-4 grid gap-3">{q.options.map(o=><button key={o} onClick={()=>onAnswer?.(o)} className={`rounded-2xl border p-4 text-left ${answer===o?'border-royal bg-blue-50':'bg-white'}`}>{o}</button>)}</div>:<textarea className="input mt-4 min-h-32" placeholder="Type your working and answer..." value={answer||''} onChange={e=>onAnswer?.(e.target.value)}/>}</div>}
