import Link from 'next/link';const links=['gcse','mocks','diagnostic','sprints','free-resources','tuition','pricing'];export function Header(){return <header className="sticky top-0 z-50 glass"><div className="mx-auto flex max-w-7xl items-center justify-between p-4"><Link href="/" className="text-2xl font-black text-navy">Grade<span className="text-royal">Sprint</span></Link><nav className="hidden gap-5 text-sm font-semibold md:flex">{links.map(l=><Link key={l} href={`/${l}`} className="hover:text-royal">{l.replace('-',' ')}</Link>)}</nav><Link href="/diagnostic" className="btn btn-primary py-2">Start diagnostic</Link></div></header>}
