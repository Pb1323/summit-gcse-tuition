import { PricingCard } from '@/components/Cards';import { pricing } from '@/data/pricing';export default function Page(){return <main className="section"><div className="mx-auto max-w-7xl"><h1 className="text-5xl font-black">Pricing</h1><p className="mt-3 text-slate-600">No fake guarantees — just clear exam practice, reports and targeted support.</p><div className="mt-8 grid gap-6 md:grid-cols-4">{pricing.map(p=><PricingCard key={p.name} p={p}/>)}</div></div></main>}
