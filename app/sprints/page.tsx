import { SprintCard } from '@/components/Cards';import { sprints } from '@/data/sprints';export default function Page(){return <main className="section"><div className="mx-auto max-w-7xl"><h1 className="text-5xl font-black">GCSE sprint courses</h1><p className="mt-3 text-slate-600">Short, targeted courses built from mock report weaknesses.</p><div className="mt-8 grid gap-6 md:grid-cols-4">{sprints.map(s=><SprintCard key={s.title} s={s}/>)}</div></div></main>}
