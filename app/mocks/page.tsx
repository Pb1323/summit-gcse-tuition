import { MockCard } from '@/components/Cards';import { mocks } from '@/data/mocks';export default function Page(){return <main className="section"><div className="mx-auto max-w-7xl"><h1 className="text-5xl font-black">GCSE mock packs</h1><p className="mt-3 text-slate-600">Timed mocks with worked solutions and reports.</p><div className="mt-8 grid gap-6 md:grid-cols-3">{mocks.map(m=><MockCard key={m.id} mock={m}/>)}</div></div></main>}
