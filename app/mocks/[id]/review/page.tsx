import { ReviewSummary } from '@/components/ReviewSummary';import { getMock } from '@/data/mocks';import { questions } from '@/data/questions';import { QuestionRenderer } from '@/components/QuestionRenderer';import { notFound } from 'next/navigation';export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const mock=getMock(id); if(!mock)notFound(); const ids=mock.sections.flatMap(s=>s.questionIds); const attempt={mockId:mock.id,studentName:'Prototype student',answers:Object.fromEntries(ids.map((id,i)=>[id,i%3?'topic method answer':''])),timeSpentSeconds:Object.fromEntries(ids.map((id,i)=>[id,80+i*8]))}; return <main className="section"><div className="mx-auto max-w-7xl"><h1 className="text-5xl font-black">Review: {mock.title}</h1><div className="mt-8"><ReviewSummary attempt={attempt}/></div><h2 className="mt-12 text-3xl font-black">Question-by-question review</h2><div className="mt-6 space-y-6">{ids.map(id=>{const q=questions.find(x=>x.id===id)!;return <div className="card p-6" key={id}><QuestionRenderer q={q}/><p className="mt-4"><b>Correct answer:</b> {q.correctAnswer}</p><p><b>Worked solution:</b> {q.workedSolution}</p><p className="text-danger"><b>Common mistake:</b> {q.commonMistake}</p></div>})}</div></div></main>}
