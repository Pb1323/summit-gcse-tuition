import { getMock } from '@/data/mocks';import Link from 'next/link';import { notFound } from 'next/navigation';export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const mock=getMock(id); if(!mock)notFound(); return <main className="section"><div className="mx-auto max-w-4xl"><div className="card p-8"><h1 className="text-5xl font-black">{mock.title}</h1><p className="mt-4 text-xl text-slate-600">{mock.description}</p><div className="mt-6 flex flex-wrap gap-2"><span className="badge">{mock.durationMinutes} min</span><span className="badge">{mock.totalMarks} marks</span><span className="badge">{mock.questionCount} questions</span></div><h2 className="mt-8 text-2xl font-black">Instructions</h2><ul className="mt-3 list-disc pl-5">{mock.instructions.map(i=><li key={i}>{i}</li>)}</ul><Link href={`/mocks/${mock.id}/start`} className="btn btn-primary mt-8">Start timed mock</Link></div></div></main>}
